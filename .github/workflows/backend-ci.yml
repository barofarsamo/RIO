name: Backend CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mongodb:
        image: mongo:latest
        ports:
          - 27017:27017
        options: >-
          --health-cmd="mongosh --eval 'db.runCommand(\"ping\").ok'"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      
      redis:
        image: redis:alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      
      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      
      - name: Run tests
        run: mvn test -Dspring.data.mongodb.uri=mongodb://localhost:27017/test -Dspring.data.redis.host=localhost
      
      - name: Build JAR
        run: mvn clean package -DskipTests

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Build JAR
        run: mvn clean package -DskipTests
      
      - name: Build Docker image
        run: |
          docker build -t riyobox-backend:${{ github.sha }} .
          docker tag riyobox-backend:${{ github.sha }} riyobox-backend:latest
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Push Docker image
        run: |
          docker tag riyobox-backend:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/riyobox-backend:${{ github.sha }}
          docker tag riyobox-backend:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/riyobox-backend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/riyobox-backend:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/riyobox-backend:latest
      
      - name: Deploy to Heroku
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
          heroku_email: ${{ secrets.HEROKU_EMAIL }}
          usedocker: true
      
      - name: Deploy to AWS ECS
        if: false # Set to true when AWS is configured
        run: |
          echo "Deploying to AWS ECS..."
          # Add AWS ECS deployment steps
