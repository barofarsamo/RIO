spring:
  data:
    mongodb:
      uri: mongodb://riyobox:riyobox123@localhost:27017/riyobox-dev?authSource=admin
  
  redis:
    host: localhost
    port: 6379
    password: redis123
  
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

logging:
  level:
    com.riyobox: DEBUG
    org.springframework.data.mongodb.core: DEBUG
    org.springframework.web.servlet: DEBUG
    org.hibernate.SQL: DEBUG

app:
  cors:
    allowed-origins: "*"
